spring:
  application:
    name: pastebin-notifications-service

  mail:
    host: smtp.gmail.com
    port: 587
    username: vvaa3174@gmail.com
    password: kvjcotnzlaymhwml
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: pastebin_user
    password: pastebin_password
    queues:
      register: register-notification.queue
    exchanges:
      register: register-notification.internal.queue
    routing-keys:
      register: register-notification.internal.routing-key
server:
  port: 8081
  error:
    whitelabel:
      enabled: false